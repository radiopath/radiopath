{{- if .Values.ingress.enabled }}
{{- if not .Values.ingress.hosts }}{{ fail "ingress.hosts must contain at least one host" }}{{ end }}
{{- $canonical := .Values.ingress.canonicalHost }}
{{- if and $canonical (not (has $canonical .Values.ingress.hosts)) }}{{ fail (printf "ingress.canonicalHost %q must be one of ingress.hosts" $canonical) }}{{ end }}
{{- /* With a canonical host this Ingress serves only that one; the others live in ingress-redirect.yaml. */}}
{{- $hosts := ternary (list $canonical) .Values.ingress.hosts (not (empty $canonical)) }}
{{- $issuer := .Values.ingress.tls.clusterIssuer }}
{{- $secretName := .Values.ingress.tls.secretName }}
{{- if and (not $secretName) $issuer }}{{ $secretName = printf "%s-tls" .Release.Name }}{{ end }}
{{- $annotations := deepCopy .Values.ingress.annotations }}
{{- with $issuer }}{{ $_ := set $annotations "cert-manager.io/cluster-issuer" . }}{{ end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  labels:
    {{- include "radiopath.labels" . | nindent 4 }}
  {{- with $annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        {{- range $hosts }}
        - {{ . | quote }}
        {{- end }}
      {{- with $secretName }}
      secretName: {{ . }}
      {{- end }}
  {{- end }}
  rules:
    {{- range $hosts }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $.Release.Name }}
                port:
                  name: http
    {{- end }}
{{- end }}
